{
  "_meta": {
    "title": "Plugins deliberately NOT in ai-kit catalog",
    "notes": "ai-kit's catalog is for COMPLEMENTARY tools, not ALTERNATIVES that overlap ai-kit's own integrated skills. During initial design (gstack, gsd, superpowers, mattpocock-skills, etc were studied), the best ideas were distilled INTO ai-kit's lifecycle. Recommending these plugins alongside ai-kit creates double-bundle noise. The audit emits KEEP-EXTERNAL with the reason from this file instead of ADOPT, so users see deliberate non-adoption (not 'promotion candidate').",
    "vendored_at": "2026-05-24"
  },
  "excluded": [
    {
      "name": "superpowers",
      "reason": "Meta-skill bundle (obra/superpowers-marketplace). Overlaps with ai-kit's /ai:tdd, /ai:review, /ai:retro, /ai:ship, /ai:plan-* and others — best patterns were distilled into ai-kit's own lifecycle during initial design. Adding it as a recommended plugin would create double-bundle noise.",
      "alternative": "Use ai-kit's own lifecycle skills."
    },
    {
      "name": "taste-skill",
      "reason": "Anti-slop frontend design pack (Leonxlnx/taste-skill, installs via `npx skills add` — not a Claude Code marketplace plugin, so it does not fit the plugins.json schema). Overlaps the existing design entries (frontend-design / lazyweb) and ui-ux-pro-max; single-source preview. Its distinctive value (GSAP motion code skeletons + AI image-board generation) is design *generation*, out of scope for what ai-kit recommends as durable per-project infra. should-i-use verdict: Ignore.",
      "alternative": "If a project specifically wants its motion/image-board generation, install directly: `npx skills add https://github.com/Leonxlnx/taste-skill`. Use ui-ux-pro-max for reasoned design-system output."
    },
    {
      "name": "designlang",
      "reason": "Live-DOM design-system EXTRACTION tool (Manavarya09/design-extract, npm `designlang`). Ships an MCP server + Claude Code plugin, so it is structurally eligible for the mcp-servers.json bucket — but it optimizes none of the AI-loop dimensions the companion catalog is for (graphify=input, caveman=output, llm-wiki=memory, context7=docs); it emits frontend artefacts (DTCG tokens / Tailwind / shadcn / Figma) from a URL. That is one-shot task tooling you run when cloning/matching a design, not durable per-project infra that compounds each session. Fails the promotion-quorum bar (single-maintainer, v12.23 high-churn, sponsor-affiliate README → 1 source, preview-only, never default-recommended). Same out-of-scope bucket as taste-skill (design generation/extraction). should-i-use verdict: Ignore.",
      "alternative": "If a frontend project genuinely needs live-site token extraction or CI drift-check against a deployed source-of-truth, install directly and wire its MCP: `npx designlang <url>` / `designlang mcp`. Use ui-ux-pro-max for reasoned design-system output."
    },
    {
      "name": "codebase-memory-mcp",
      "reason": "Code-intelligence MCP server (DeusData/codebase-memory-mcp, pure-C static binary, MIT). Builds a persistent knowledge graph via tree-sitter (158 langs) + 'Hybrid LSP' type resolution; 14 MCP tools (search_graph, trace_path, Cypher query_graph, semantic search, cross-service HTTP linking). Structurally eligible for the mcp-servers.json bucket, but it is an ALTERNATIVE to graphify (ai-kit's knowledge-graph/input companion, wired into CLAUDE.md + query workflow + PreToolUse hook) — not complementary. Its own README says it is 'similar in spirit to graphify's graphify-out/'. Two knowledge-graph tools = double-bundle noise. Adopting it also runs against ADR-0006 (ai-kit deliberately dropped MCP; stays pure Bash+Markdown, minimal footprint) and its curl|bash install auto-wires config + hooks across 11 agents. Fails promotion-quorum like designlang/taste-skill: single org, README = sole source. should-i-use verdict: Ignore. Whether it should REPLACE graphify (stronger engine on paper) is a separate deliberate spike, not a catalog add — tracked in #109.",
      "alternative": "Keep graphify as the knowledge-graph companion. If a project specifically wants type-aware call graphs / semantic / Cypher search, install directly and wire its MCP per the upstream README. Migration decision: see issue #109."
    },
    {
      "name": "headroom",
      "reason": "Context-compression layer for AI agents (headroomlabs-ai/headroom, Apache-2.0, ~47k stars, active). Proxy + MCP + library that compresses tool-outputs/logs/RAG/files 60-95% before the LLM, plus an output-shaper (verbosity steering + effort routing). In-category (token efficiency) and popular — NOT wrong-category like the design tools — but Ignored as a blanket companion on three grounds: (1) footprint vs ADR-0006 — Python 3.10+ + HuggingFace model (Kompress-v2-base) + optional pytorch-mps + long-running proxy, against ai-kit's pure-Bash+Markdown / MCP-dropped / minimal posture; (2) trust surface — the proxy sits in the path of all prompts+responses, exchanges provider/Copilot OAuth tokens, and `headroom learn` auto-edits CLAUDE.md/AGENTS.md (the files ai-kit's own loops own); lossy pre-agent compression is a correctness gamble vs ai-kit's verifiability ethos; (3) dimension-spanning overlap — its output-shaper dubbelt caveman, its cross-agent memory/learn dubbelt llm-wiki + the checkpoint loop. Only its proxy tool-output compression is genuinely new (graphify indexes code, not a 65k-token log). should-i-use verdict: Ignore. Whether that one novel angle earns a 'runtime-output' companion entry is a separate deliberate spike, not a catalog add — tracked in #110.",
      "alternative": "Keep graphify (code input) + caveman (output) as the orthogonal companions. If a specific project drowns in large runtime tool-outputs (SRE logs, big grep/RAG dumps) AND accepts the proxy trust surface, install directly and wire its MCP per upstream: `pip install headroom-ai[all]` / `headroom mcp install`. Adoption decision: see issue #110."
    },
    {
      "name": "skillspector",
      "reason": "Security scanner for AI agent skills (NVIDIA/SkillSpector, Apache-2.0, Python 3.12+). 68 vuln patterns across 17 categories (prompt injection, data exfiltration, privilege escalation, supply chain, MCP tool poisoning, …), two-stage static + optional-LLM, SARIF/JSON/MD output, scans repo/URL/zip/dir/file. Ships a CLI + an MCP server (`scan_skill`), so structurally eligible for the mcp-servers.json bucket — but it is one-shot scanner tooling, NOT a per-session compounding companion like the catalog is for (graphify=input, caveman=output, llm-wiki=memory, context7=docs). Footprint (Python + optional-LLM API spend + MCP/long-running scan) runs against ADR-0006 (pure Bash+Markdown, MCP-dropped, no runtime daemon), same posture objection as headroom/codebase-memory-mcp. So it does NOT belong as a recommend-to-everyone companion. UNLIKE the prior Ignores it is neither wrong-category (taste-skill/designlang) nor a duplicate-of-existing-companion (codebase-memory-mcp/headroom): it plugs a real hole in ai-kit's OWN trust machinery — VETTING.md's seven criteria have no automated security/malicious-pattern dimension, yet ai-kit emits 27 skills + recommends 12 plugins under an implicit 'safe to wire' endorsement. NVIDIA-backed + Apache-2.0 clears the single-maintainer/license quorum concerns that sank the others. should-i-use verdict: Ignore (catalog) / Wire-as-internal-tool. Whether to wire `skillspector scan --no-llm` (static, zero-API) as an advisory CI gate on ai-kit's emitted skills + a probe step in should-i-use/recommend-rules is a deliberate spike, not a catalog add — tracked in #111.",
      "alternative": "Don't add to the companion catalog. If wiring as ai-kit's own security gate proves out (#111), it lives in CI + the vetting pipeline, not in a recommend-to-everyone table. To scan a single skill ad-hoc today: `uvx --from git+https://github.com/NVIDIA/skillspector skillspector scan <dir> --no-llm` or via the bundled `scan_skill` MCP tool."
    }
  ]
}
