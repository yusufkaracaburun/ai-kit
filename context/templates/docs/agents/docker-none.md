# Docker

This project does **not** use Docker for local development.

Run commands on the host. Detect install/test commands from repo files and **official documentation** (see `docs/agents/dev-environment.md`).

**Note:** Sandcastle AFK agents still use Docker for agent isolation — that is separate from app dev.
