---
description: ai-kit project context — read AGENTS.md and CONTEXT.md before implementation
globs:
alwaysApply: true
---

# ai-kit

- Read `AGENTS.md` for skill routing and agile lifecycle.
- Read `CONTEXT.md` for domain language before coding.
- Skills: `.cursor/skills/` (project) or `~/.cursor/skills/` (global).
- Run `/setup` if `docs/agents/` or `.ai-kit-setup` is missing.
- Stack-agnostic: use `docs/agents/dev-environment.md` and official docs for commands.
