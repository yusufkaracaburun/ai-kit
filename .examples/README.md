# Upstream staging (removed)

Upstream repos were cloned here during initial curation and have been deleted.

Provenance and commit pins are documented in the root [README.md](../README.md) Provenance section.

To refresh from upstream in the future, clone manually into a temp directory and curate into `workflow/`, `standards/`, and `orchestration/`.
