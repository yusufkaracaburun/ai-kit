# Env-prod that trips L19, L20.
APP_ENV=production
# L19: Queue driver = sync in non-local env (🟠) — jobs run on request thread.
QUEUE_CONNECTION=sync
# L20: Database driver = sqlite in non-local env (🟠) — concurrency lock-up risk.
DB_CONNECTION=sqlite
